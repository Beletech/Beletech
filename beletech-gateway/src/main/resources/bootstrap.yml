spring:
  cloud:
    discovery:
      reactive:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true
    nacos:
      discovery:
        server-addr: http://127.0.0.1
        namespace: beletech
      config:
        server-addr: http://127.0.0.1
        namespace: beletech
