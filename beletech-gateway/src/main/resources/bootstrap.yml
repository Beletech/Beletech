spring:
  cloud:
    discovery:
      reactive:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: false
    nacos:
      discovery:
        server-addr: http://127.0.0.1:8848
        namespace: beletech
      config:
        server-addr: http://127.0.0.1:8848
        namespace: beletech
